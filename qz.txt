<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Coming Soon</title>
  <meta name="description" content="We’re launching soon. Join the list to stay informed." />
  <style>
    :root{
      --bg: #0b1020;
      --card: rgba(255,255,255,.08);
      --text: rgba(255,255,255,.92);
      --muted: rgba(255,255,255,.68);
      --border: rgba(255,255,255,.18);
      --accent: #7c5cff;
      --accent2:#3dd6d0;
      --shadow: 0 20px 60px rgba(0,0,0,.35);
      --radius: 18px;
    }
    *{ box-sizing:border-box; }
    body{
      margin:0;
      min-height:100svh;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      color: var(--text);
      background:
        radial-gradient(1000px 600px at 20% 10%, rgba(124,92,255,.30), transparent 60%),
        radial-gradient(900px 600px at 85% 70%, rgba(61,214,208,.22), transparent 55%),
        linear-gradient(180deg, #070a14, var(--bg));
      display:grid;
      place-items:center;
      padding: 24px 16px;
    }
    .wrap{
      width: min(720px, 100%);
    }
    .card{
      background: var(--card);
      border: 1px solid var(--border);
      box-shadow: var(--shadow);
      border-radius: var(--radius);
      padding: clamp(20px, 5vw, 44px);
      backdrop-filter: blur(10px);
    }
    .badge{
      display:inline-flex;
      gap:10px;
      align-items:center;
      padding: 8px 12px;
      border: 1px solid var(--border);
      border-radius: 999px;
      color: var(--muted);
      font-size: 14px;
      margin-bottom: 18px;
    }
    .dot{
      width:10px;height:10px;border-radius:50%;
      background: linear-gradient(90deg, var(--accent), var(--accent2));
      box-shadow: 0 0 18px rgba(124,92,255,.55);
    }
    h1{
      margin: 0 0 10px;
      font-size: clamp(30px, 5.6vw, 56px);
      letter-spacing: -0.02em;
      line-height: 1.05;
    }
    p{
      margin: 0 0 22px;
      color: var(--muted);
      font-size: clamp(16px, 2.3vw, 18px);
      line-height: 1.55;
      max-width: 56ch;
    }
    .form{
      display:grid;
      gap: 12px;
      grid-template-columns: 1fr;
      margin-top: 8px;
    }
    .field{
      display:flex;
      gap: 10px;
      padding: 10px;
      border-radius: 14px;
      border: 1px solid var(--border);
      background: rgba(0,0,0,.18);
    }
    input[type="email"]{
      border:0;
      outline:0;
      background: transparent;
      color: var(--text);
      width:100%;
      font-size: 16px; /* prevents iOS zoom */
      padding: 10px 8px;
    }
    input::placeholder{ color: rgba(255,255,255,.45); }
    button{
      border:0;
      cursor:pointer;
      border-radius: 14px;
      padding: 12px 16px;
      font-weight: 650;
      font-size: 16px;
      color: #0b1020;
      background: linear-gradient(90deg, var(--accent), var(--accent2));
      box-shadow: 0 12px 30px rgba(124,92,255,.22);
      transition: transform .08s ease, filter .2s ease;
      width: 100%;
    }
    button:active{ transform: translateY(1px) scale(.99); }
    button:focus-visible, input:focus-visible{
      outline: 3px solid rgba(124,92,255,.45);
      outline-offset: 2px;
      border-radius: 14px;
    }
    .fineprint{
      margin-top: 10px;
      font-size: 13px;
      color: rgba(255,255,255,.55);
    }
    .status{
      margin-top: 10px;
      font-size: 14px;
      min-height: 1.2em;
    }
    .status.ok{ color: rgba(61,214,208,.95); }
    .status.err{ color: rgba(255,140,140,.95); }

    /* wider screens: put button next to input */
    @media (min-width: 560px){
      .form{
        grid-template-columns: 1fr auto;
        align-items: center;
      }
      button{
        width: auto;
        padding: 12px 18px;
        white-space: nowrap;
      }
    }
  </style>
</head>

<body>
  <main class="wrap">
    <section class="card" aria-labelledby="title">
      <div class="badge"><span class="dot" aria-hidden="true"></span> Launching soon</div>
      <h1 id="title">We’re building something new.</h1>
      <p>
        Our website is coming soon. Drop your email and we’ll let you know when we’re live.
      </p>

      <!-- Replace action with your endpoint (Mailchimp, ConvertKit, etc.) -->
      <form class="form" id="notifyForm" method="post" action="#" novalidate>
        <div class="field">
          <label for="email" class="sr-only" style="position:absolute;left:-9999px;">Email address</label>
          <input
            id="email"
            name="email"
            type="email"
            inputmode="email"
            autocomplete="email"
            placeholder="you@example.com"
            required
          />
        </div>
        <button type="submit">Notify me</button>
      </form>

      <div class="status" id="status" role="status" aria-live="polite"></div>
      <div class="fineprint">No spam — only an update when we launch.</div>
    </section>
  </main>

  <script>
    const form = document.getElementById("notifyForm");
    const email = document.getElementById("email");
    const statusEl = document.getElementById("status");

    function setStatus(msg, type) {
      statusEl.textContent = msg;
      statusEl.className = "status " + (type || "");
    }

    form.addEventListener("submit", async (e) => {
      e.preventDefault();
      setStatus("");

      const value = email.value.trim();

      // Basic validation
      if (!value) return setStatus("Please enter your email address.", "err");
      if (!email.checkValidity()) return setStatus("That email address doesn’t look right.", "err");

      // TODO: Send to your backend / email service.
      // Example: fetch("/api/subscribe", { method:"POST", headers:{ "Content-Type":"application/json" }, body: JSON.stringify({ email: value }) })

      // Demo success state
      setStatus("Thanks! You’re on the list.", "ok");
      form.reset();
      email.focus();
    });
  </script>
</body>
</html>
